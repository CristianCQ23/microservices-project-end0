services:
  api:
    container_name: c-customers
    image: customers-api:latest
    build: .
    ports:
      - "8001:8001"
    environment:
      PORT: 8001
      DB_URL: mongodb://mongo:27017/customers
      APP_SECRET: ${APP_SECRET:-dev-secret-change-me}
    depends_on:
      - mongo

  mongo:
    container_name: backend-db
    image: mongo:7
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: